1）bnum 和 blen
bnum=0：命令参数为空，是合法的命令结构，表示没有参数。
blen=0：单个参数为空字符串，协议或业务上不允许，防止错误输入或语义不明确。
->
*0\r\n
参数数量为0，意味着没有任何参数，服务器直接处理空命令参数，这是允许的。
*1\r\n$0\r\n\r\n
参数数量为1，但参数长度为0，意味着参数是空字符串，这个协议代码不允许（返回错误），因为空参数可能会导致程序逻辑不一致或不可预期。